use components::prelude::*;
use render::prelude::*;
pub struct UmbrellaPdfRender;
impl Layout<String, Sections, Theme> for UmbrellaPdfRender {
    fn render(
        &self,
        sections: Sections,
        shade: &Theme,
    ) -> String {
        todo!()
    }
}

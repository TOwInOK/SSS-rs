<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link href="./card.css" rel="stylesheet">
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=PT+Mono&display=swap" rel="stylesheet">
</head>
  <body class="min-h-screen bg-[{{ shade.colors.secondary }}] flex justify-center items-center p-4 overflow-clip">
    <div class="max-w-md w-full bg-black/20 rounded-2xl border border-black p-1 overflow-scroll-x">
        <div class="p-4 rounded-lg border-t-2 border-b-2 border-[{{ shade.colors.thirdly }}] flex flex-col gap-4">

        <div class="flex justify-evenly items-center text-[{{ shade.colors.primary }}]">
            <div class="text-lg md:text-xl font-bold font-mono">
                Some({{ user.nickname.word }})
            </div>
            <div class="flex items-center justify-evenly w-full max-w-1/3 text-2xl md:text-3xl font-mono">
                <span>[</span>
                <div class="flex flex-col items-center">
                    <span class="text-xs md:text-sm font-mono">{{ user.nickname.word }}</span>
                    <span class="text-[10px] md:text-xs text-[{{ shade.colors.thirdly }}]/80 font-mono tracking-widest">
                        {{ user.nickname.pronounce  }}
                    </span>
                </div>
                <span>]</span>
            </div>
        </div>



        <div class="grid grid-cols-2 gap-2 md:gap-4">
            <div class="grid gap-2">
                {% for spec in specs %}
                    <div class="text-[{{ shade.colors.primary }}] flex items-center max-w-[120px] justify-between">
                        <span class="text-xl md:text-2xl font-mono w-[15px]">[</span>
                        <span class="text-xs md:text-sm font-mono text-center flex-1">{{ spec }}</span>
                        <span class="text-xl md:text-2xl font-mono w-[15px]">]</span>
                    </div>
                {% endfor %}
            </div>
            <div class="text-[{{ shade.colors.thirdly }}] text-right font-mono text-sm md:text-base max-h-[{{ about.max_length }}px]">
                {{ about.text }}
            </div>
        </div>

            <div class="flex justify-evenly w-full text-[{{ shade.colors.primary }}]">
                {% for social in socials %}
                    <div class="flex items-center justify-evenly w-full">
                        <span class="text-xl md:text-2xl font-mono">[</span>
                        <div class="h-[48px] flex-col justify-evenly items-center flex">
                            <svg xmlns="http://www.w3.org/2000/svg" class="text-[{{ shade.colors.thirdly }}] size-12 md:size-20" {{ social.logo }}
                            </svg>
                            <div class="flex">
                              <a href="{{ social.link }}" class="text-xs md:text-sm font-mono">
                                  {{ social.provider }}
                              </a>
                            </div>
                        </div>
                        <span class="text-xl md:text-2xl font-mono">]</span>
                    </div>
                {% endfor %}
            </div>

            <div class="flex justify-evenly gap-4 justify-evenly">
                {% for skill in skills %}
                    <div class="text-center font-mono">
                        <div class="text-xl md:text-2xl text-[{{ shade.colors.primary }}]">
                            {{ skill.skill }}
                        </div>
                        {% if skill.since %}
                            <div class="flex justify-center  text-xs md:text-sm text-[{{ shade.colors.thirdly }}]">
                                {{ skill.since.start  }} -> {% if skill.since.end != 0 %} {{ skill.since.end }} {% else %} today {% endif %}
                             </div>
                        {% else %}{% endif %}
                        {% if skill.site_label %}
                          {% if skill.site_link %}
                            <a href="{{ skill.site_link }}" class="flex justify-center text-xs md:text-sm text-[{{ shade.colors.border }}]">
                                {{ skill.site_label }}
                            </a>
                          {% else %}
                            <div class="flex justify-center text-xs md:text-sm text-[{{ shade.colors.border }}]">
                                {{ skill.site_label }}
                            </div>
                          {% endif %}
                        {% else %}
                        {% endif %}
                    </div>
                {% endfor %}
            </div>

        </div>
    </div>
  </body>
</html>

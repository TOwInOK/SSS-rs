use components::prelude::*;
use render::prelude::*;
pub struct UmbrellaImgRender;
impl Layout<String, Sections, Theme> for UmbrellaImgRender {
    fn render(
        sections: Sections,
        shade: &Theme,
    ) -> String {
        todo!()
    }

    fn finylize(
        rendered: String,
        shade: &Theme,
    ) -> String {
        todo!()
    }
}

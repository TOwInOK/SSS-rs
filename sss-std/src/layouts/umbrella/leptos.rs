use components::prelude::*;
use leptos::prelude::AnyView;
use render::prelude::*;
pub struct UmbrellaLeptosRender;
impl Layout<AnyView, Sections, Theme> for UmbrellaLeptosRender {
    fn render(
        sections: Sections,
        shade: &Theme,
    ) -> AnyView {
        todo!()
    }

    fn finylize(
        rendered: AnyView,
        shade: &Theme,
    ) -> AnyView {
        todo!()
    }
}
